install -g @angular/cli@10.1.6
ng version
ng new filmes
cd filmes
ng serve || ng s -o

Entrar na pasta do arquivo json -> dados
executar os comandos:

npm install -g json-server
json-server --watch db.json
